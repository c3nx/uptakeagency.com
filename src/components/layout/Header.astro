---
import Container from "@/components/ui/Container.astro";
import LanguageSwitcher from "@/components/ui/LanguageSwitcher.astro";
import Button from "@/components/ui/Button.astro";
import { t, getLocalizedPath } from "@/i18n";

interface Props {
  locale: "en" | "tr";
  currentPath: string;
}

const { locale, currentPath } = Astro.props;
const i18n = t(locale);

const navItems = [
  { label: i18n.nav.about, path: "/about" },
  { label: i18n.nav.services, path: "/services" },
  { label: i18n.nav.work, path: "/work" },
  { label: i18n.nav.contact, path: "/contact" },
];
---

<header class="fixed top-0 z-50 w-full border-b border-surface-border bg-surface/80 backdrop-blur-lg">
  <Container>
    <nav class="flex h-16 items-center justify-between">
      <!-- Logo -->
      <a href={getLocalizedPath("/", locale)} class="flex items-center gap-2 text-text-primary">
        <img src="/favicon.svg" alt="Uptake Agency" class="h-8 w-8" />
        <span class="font-mono text-sm font-semibold">uptake.agency</span>
      </a>

      <!-- Desktop Nav -->
      <div class="hidden items-center gap-8 md:flex">
        {navItems.map((item) => (
          <a
            href={getLocalizedPath(item.path, locale)}
            class:list={[
              "text-sm font-medium transition-colors",
              currentPath === item.path
                ? "text-brand-400"
                : "text-text-muted hover:text-text-primary",
            ]}
          >
            {item.label}
          </a>
        ))}
        <LanguageSwitcher locale={locale} currentPath={currentPath} />
        <Button href={`mailto:info@uptakeagency.com`} size="sm">
          {i18n.contact.cta}
        </Button>
      </div>

      <!-- Mobile Menu Button -->
      <button
        id="mobile-menu-btn"
        class="flex items-center md:hidden"
        aria-label="Toggle menu"
        type="button"
      >
        <svg class="h-6 w-6 text-text-secondary" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
        </svg>
      </button>
    </nav>
  </Container>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="hidden border-t border-surface-border bg-surface md:hidden">
    <Container>
      <div class="flex flex-col gap-4 py-6">
        {navItems.map((item) => (
          <a
            href={getLocalizedPath(item.path, locale)}
            class="text-base font-medium text-text-secondary hover:text-text-primary"
          >
            {item.label}
          </a>
        ))}
        <div class="flex items-center gap-4 border-t border-surface-border pt-4">
          <LanguageSwitcher locale={locale} currentPath={currentPath} />
          <Button href={`mailto:info@uptakeagency.com`} size="sm">
            {i18n.contact.cta}
          </Button>
        </div>
      </div>
    </Container>
  </div>
</header>

<script>
  const btn = document.getElementById("mobile-menu-btn");
  const menu = document.getElementById("mobile-menu");
  btn?.addEventListener("click", () => {
    menu?.classList.toggle("hidden");
  });
</script>
